
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus  # 必须和仪表盘 JSON 中的 datasource uid 一致
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
